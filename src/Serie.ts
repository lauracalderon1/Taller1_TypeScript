export class Serie {
    constructor(public id: number, public nombre: string, public plataforma: string, public temporadas: number, public descripcion: string, public link:string, public imagen : string){}
}